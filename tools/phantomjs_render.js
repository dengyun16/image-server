var system  = require('system');var webpage = require('webpage');var defaultOpts = {  viewportSize: {    width : 1920,    height: 1028  },  clipRect    : {    top   : 0,    left  : 0,    width : 1920,    height: 1028  },  settings    : {    javascriptEnabled: true,    loadImages       : true,    userAgent        : 'Mozilla/5.0 (Windows NT 6.1) AppleWebKit/537.31 (KHTML, like Gecko) PhantomJS/19.0',    //userAgent        : 'Mozilla/5.0 (Windows NT 6.1; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/46.0.2490.71 Safari/537.36'  }};if (system.args.length <= 1) {  phantom.exit(1);}else {  var address    = system.args[1];  var savePath   = system.args[2];  var customOpts = system.args[3] || {};  for (var i in customOpts) {    if (customOpts[i]) {      opts[i] = customOpts[i];    }  }  var page = webpage.create();  page.viewportSize = defaultOpts.viewportSize/* || defaultOpts.viewportSize*/;  page.clipRect = defaultOpts.clipRect/* || defaultOpts.clipRect*/;  page.settings = defaultOpts.settings;  page.open(address, function (status) {    if (status === 'fail') {      console.error('pageOpenFail');    }    else {      page.render(savePath);    }    page.close();    phantom.exit(0);  });}